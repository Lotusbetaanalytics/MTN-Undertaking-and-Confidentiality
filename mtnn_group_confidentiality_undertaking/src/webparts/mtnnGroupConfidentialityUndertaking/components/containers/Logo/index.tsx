import * as React from 'react';
import styles from './styles.module.scss';

const Logo = () => {
    return(
        <span className={styles.logo}>
            <span>MTN</span>
        </span>
    );
};

export default Logo;